# Build-time dependencies for the setup executable.
#
# Deliberately tiny: the bootstrapper downloads everything else, so the only
# thing it needs itself is a toolkit to draw the wizard and PyInstaller to
# freeze it.  Networking and unpacking come from the standard library.

PySide6>=6.7,<7
pyinstaller>=6.6
