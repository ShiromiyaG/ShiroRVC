"""Windows setup executable for ShiroRVC.

Like ``gui/``, this directory is self-contained: it imports nothing from the
application, because it runs before the application is on disk.  Deleting it
removes the packaging pipeline and nothing else.
"""
